# Claude Code (local agent state — never commit)
.claude/
.claude-cache/
CLAUDE.local.md

# OS noise
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.history/

# Env files (defensive — also ignored per-package)
.env
.env.local
.env.*.local

# Dependencies (defensive)
node_modules/

# Build output (defensive)
.next/
dist/
build/
out/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime / session memory (file-backed JSON store)
data/sessions.json

# Coverage & misc
coverage/
.turbo/

# Local project notes (never commit)
project.md
