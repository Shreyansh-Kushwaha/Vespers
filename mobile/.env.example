# Base URL of the Vespers backend (Hono server). For local dev with Expo Go on a
# physical device, use your machine's LAN IP, not localhost — the phone needs
# to reach your laptop. Example: http://192.168.1.42:8787
EXPO_PUBLIC_API_BASE=http://localhost:8787
