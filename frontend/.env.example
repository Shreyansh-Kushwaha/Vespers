# Where the Vespers backend is reachable from the browser.
# Local dev:  http://localhost:8787
# Production: your deployed backend URL (e.g. https://vespers-api.fly.dev)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8787
